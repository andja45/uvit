<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ispiti</title>
</head>
<body>

    <h1>Ispiti za studenta <%= username %></h1>

    <% if(exams.length === 0) { %>
        <p>student nije polagao ispite</p>
    <% } else { %>
        <table>
            <thead>
                <tr>
                    <th>Predmet</th>
                    <th>Datum</th>
                    <th>Ocena</th>
                </tr>

            </thead>
            <tbody>
                <% for(exam of exams) { %>
                    <tr>
                        <td><%= exam.subject %></td>
                        <td><%= exam.date.getDay() %>.<%= exam.date.getMonth() + 1 %>.<%= exam.date.getFullYear() %>.</td> <!-- FORMATIRANJE DATUMA!, getMonth()+1 jer vraca [0-11], getFullYear() se zove!! -->
                        <td><%= exam.grade %></td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    <% } %>
    
</body>
</html>